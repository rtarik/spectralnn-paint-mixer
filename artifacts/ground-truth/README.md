# Ground-Truth Data

Contributor-facing paint-mix targets for evaluating and fine-tuning future artifacts.

This corpus is distinct from `artifacts/fixtures`:

- `artifacts/fixtures` are compact derived regression sets for runtime parity
- `artifacts/ground-truth` is the richer append-only source corpus for reviewed targets

Current staged sets:

- `v1/`
