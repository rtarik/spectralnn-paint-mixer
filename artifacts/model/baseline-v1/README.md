# Baseline Model Artifact

This directory will contain the canonical model artifact for the frozen baseline.

Planned file:

- `model.json`

Planned contents:

- runtime contract
- mixing parameter defaults
- normalization statistics
- network weights and biases
- provenance and training summary metadata

